{
  "name": "nightwatch",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rollup -c --environment BUILD:RELEASE",
    "start": "rollup -c -w --environment BUILD:DEBUG",
    "compile-for-test": "rollup -c --environment BUILD:TEST",
    "cold-start": "rollup -c --environment BUILD:DEBUG"
  },
  "dependencies": {
    "buffer": "^5.0.0",
    "core-js": "^2.4.1",
    "react": "16.0.0-alpha.12",
    "react-native": "0.46.4",
    "react-native-image-picker": "^0.25.1",
    "react-native-onesignal": "^1.2.3"
  },
  "devDependencies": {
    "rollup": "^0.42.0",
    "rollup-plugin-commonjs": "^8.0.2",
    "rollup-plugin-fable": "^1.0.6",
    "rollup-watch": "^4.0.0"
  }
}
